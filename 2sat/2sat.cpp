#include "2sat.h"
#include "../Graph/graph.h"

/*
Variable :: Variable(string label, bool negative) {
    this->label = label;
    this->negative = negative;
}


bool solve_2sat(vector <std::pair <Variable, Variable>> disjunkts) {

    for (const auto& it : disjunkts) {
        vertex <int> Vertex(((it.first.negative) ? "not" : "") + it.first.label);
        cout << ((it.first.negative) ? "not" : "") + it.first.label << endl;
    }
	return true;
}
*/
